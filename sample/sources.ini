# Sample config - points at the bundled demo data.
# Copy sources.example.ini to sources.ini for your own feeds.
[console]
base_path = feeds

[sources]
tenant      = tenant.json
run_summary = run-summary.json
security    = security-snapshot.json
licensing   = licensing.json
history     = history
fleet       = fleet.db
